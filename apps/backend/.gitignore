node_modules
dist
.data
.nitro
.cache
.output
.vscode

# Environment — never commit real .env
.env
!.env.example

# Metrics
.metrics.json
.metrics_daily.json
.metrics_weekly.json
.metrics_monthly.json

# Prisma generated client
generated

# Lock files (managed at monorepo root)
pnpm-lock.yaml